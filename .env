# Root .env should NOT contain production secrets for the client.
# Move your server-side Gemini API key to server/.env and never commit it.
# Example (copy from server/.env.example):
# GEMINI_API_KEY=your_server_side_gemini_key_here
# SERVER_CLIENT_TOKEN=replace_with_strong_token
